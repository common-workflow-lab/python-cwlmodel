"""
Autogenerated CWL SDK
CWL version: {{ cwl_version }}
"""

{% for class in classes %}
class {{ class.name }}{% if class.extends %}({{ class.extends }}){% endif %}:
    """
    {{ class.docstring }}
    """
    def __init__(self, {% for param in params %} {% if param.type != "array" %} {{ param.name }}=None, {% endif %}{% endfor %}):
        """
        {% for param in params %}
        :param {{ param.name }}: {{ param.docstring }}
        :type {{ param.type }}: {{ param.type }}
        {% endfor %}
        """

        {% for param in params %}
        self.{{ param.name }} = {% if param.type == "array" %}[]{% else %}{{ param.name }}{% endif %}
        {% endfor %}

    def load
{% endfor %}
